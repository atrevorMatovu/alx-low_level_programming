#include "main.h"

/**
 * print_number - prints an integer
 * @n: the number to be output
 */
void print_number(int n)
{
	if (n < 0 || n > 0)
	{
		_putchar(n + '0');
	}
	else
	{
		_putchar(n + '0');
	}
	_putchar('\n');
}
